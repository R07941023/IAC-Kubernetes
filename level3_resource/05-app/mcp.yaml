apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://gitlab.mydormroom.dpdns.org/YYLUI/mcp.git
    targetRevision: main
    path: deployment
    kustomize: {}
  destination:
    server: https://kubernetes.default.svc
    namespace: ai
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
